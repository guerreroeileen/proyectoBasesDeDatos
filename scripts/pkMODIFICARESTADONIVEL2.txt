create or replace PACKAGE pkRegistroEstadoNivel2 AS
PROCEDURE pModificarEstado(ivCodigo IN VARCHAR2, ivNombre IN VARCHAR2);


PROCEDURE pModificarEstado(ivCodigo IN VARCHAR2, ivNombre IN VARCHAR2)
  IS
  BEGIN 
      PKESTADO.PMODIFICAR(ivCodigo IN VARCHAR2, ivNombre IN VARCHAR2);
  EXCEPTION
  WHEN OTHERS THEN
  RAISE_APPLICATION_ERROR(-20000, 'Error al insertar en la tabla Funcionario'||SQLCODE);
  END pModificarEstado